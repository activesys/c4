# C4 功能测试用例

> 本文件记录 C4 的功能测试用例，每条用例说明：**测试输入**、**测试特性**、**曾经出现的问题**、**修复的版本**。
> 用例对应「曾经暴露的缺陷」，用于缺陷修复后的回归验证。

---

## 用例 1：用户文本描述 ASFP2 接入与转发信息（内容齐全）✅

- **测试特性**：用户通过一条文本一次性提供全部接入信息——设备与协议、10 个点的「地址:点名」、接收监听端口（9001）、转发目标（127.0.0.1:9900）与转发点表（5000~5009）
- **修复的版本**：C4H1

### 测试输入

```
现在需要接入1号风机的数据，第三方厂家通过asfp2协议给我们转来1#风机数据，10个点，从1000到1009，10个点分别是1000:风速、1001:功率、1002:风向、1003:桨叶角度、1004:发电机转速、1005:齿轮箱油温、1006:塔筒温度、1007:空气温度、1008:空气湿度、1009:大气压强，使用端口9001。我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9900，点表5000~5009。
```

> 关键点：点名、接收端口、转发目标、转发点表全部由用户提供，系统必须**原样采纳**——
> 不得自行编造转发地址、不得改动用户指定的端口，方案摘要须逐条列出「地址 ↔ 点名」映射与接收/转发端口供核对。
> 转发协议亦由用户明确提供（2026-09-05 起接收/转发协议逐侧必供——用户为接收侧声明的协议仅对接收侧生效，
> 转发协议缺失时 Agent 必须询问，禁止沿用接收侧协议或按转发目标描述推断）。

### 曾经出现的问题

C4H1 之前：转发点业务字段（addr）未采纳用户提供值，而是被系统自动编造（从 3001 起顺序编址）；
用户指定的接收端口在执行阶段未正确传递（`output_plan_steps` 首次调用因缺 ip/port 校验失败后重试）；
方案摘要不展示端口，用户无法核对。修复后：用户提供的点表、端口、转发点表原样进入 config.json。

---

## 用例 2：消息含与配置场站一致的场站名（归属校验）✅

- **测试特性**：条件完整，消息中的场站名「华能阿拉善风电场」与 agent.json 配置的场站「华能阿拉善」一致；用户文本完整描述接入与转发信息（点名、接收端口 9001、转发目标 127.0.0.1:9900、转发点表 5000~5009）
- **修复的版本**：C4H1

### 测试输入

```
现在需要接入华能阿拉善风电场1号风机的数据，第三方厂家通过asfp2协议给我们转来1#风机数据，10个点，从1000到1009，10个点分别是1000:风速、1001:功率、1002:风向、1003:桨叶角度、1004:发电机转速、1005:齿轮箱油温、1006:塔筒温度、1007:空气温度、1008:空气湿度、1009:大气压强，使用端口9001。我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9900，点表5000~5009。
```

> 关键点：消息带场站前缀「华能阿拉善风电场」，与当前场站一致 → 归属校验通过，直接接入；
> 不得因场站信息再次询问，也不得误判为「不属于当前场站」。

### 曾经出现的问题

C4H1 之前：LLM 在场站已设置的情况下仍反复询问场站名称与缩写（提示词两分支规则遵从性差，
且系统提示残留未替换的 `{{ current_site }}` 占位符，被模型解读为「场站未设置」）；用户另给场站名
（如「开鲁」）时被照单全收，违反「实例绑定唯一场站」。修复后：场站归属校验确定性执行——
场站已绑定时禁止询问，消息含一致场站名 → 直接接入，含其他场站名 → 回复「该资料不属于当前场站」。

---

## 用例 3：消息含与配置场站不同的场站名（归属拒绝）✅

- **测试特性**：条件完整，但消息中的场站名「华能通辽风电场」与 agent.json 配置的场站「华能阿拉善」**不同**；其余接入信息（点名、接收端口 9001、转发目标 127.0.0.1:9900、转发点表 5000~5009）齐全
- **修复的版本**：C4H1

### 测试输入

```
现在需要接入华能通辽风电场1号风机的数据，第三方厂家通过asfp2协议给我们转来1#风机数据，10个点，从1000到1009，10个点分别是1000:风速、1001:功率、1002:风向、1003:桨叶角度、1004:发电机转速、1005:齿轮箱油温、1006:塔筒温度、1007:空气温度、1008:空气湿度、1009:大气压强，使用端口9001。我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9900，点表5000~5009。
```

> 关键点：一个 C4 实例绑定唯一场站。消息场站与当前场站不同 → 必须回复「该资料不属于当前场站」并停止：
> 不得接入、不得生成方案、不得切换场站，即使其余接入信息完整也不例外。

### 曾经出现的问题

C4H1 之前：LLM 在场站已绑定时仍反复询问场站信息；用户另给场站名（如「开鲁」）时被照单全收并完成接入，
场站被静默切换，违反「实例绑定唯一场站」。修复后：场站归属校验确定性执行——
消息含其他场站名 → 直接回复「该资料不属于当前场站（当前场站：X）」并停止，不调用任何工具。

---

## 用例 4：转发点表描述模糊，Agent 多次询问与推断后接入（升压站）✅

- **测试特性**：新设备类型（升压站）；接收点表用「:」与「是」两种混述的松散文本给出（非表格）；**转发点表描述模糊**——仅说「点表与I区一致」，未逐点给出转发地址；Agent 通过多次询问与推断确定转发点表后接入成功
- **修复的版本**：C4H1

### 测试输入

```
现在需要接入升压站的数据，第三方厂家转过来的，asfp2协议，2390:电网频率，2391是正向有功，2392是反向有功，2393是正向无功，2394是反向无功，2395是uab，2396是ubc，2397是uac，2398是变压器油温，2399是环境温度。II服务器地址是127.0.0.1:9900，点表与I区一致。
```

> 关键点：转发点的 addr 是必要项，但用户仅以「与I区一致」的等价描述给出——Agent 必须通过
> 询问/推断明确每个转发点的地址（2390~2399 一一对应），**不得自行编造**；接收点表的松散文本
>（「:」与「是」混述）应被正确解析为 10 个点。

### 曾经出现的问题

C4H1 之前：转发点业务字段缺失时被系统自动编造（3001 起顺序编址），用户提供的等价描述
（「点表与I区一致」）无法被采纳。修复后：必要项缺失时 Agent 逐项询问，结合用户回答与接收侧
点表推断出转发点表（2390~2399 一一对应），多次交互后接入成功。

---

## 用例 5：未提供转发点表，用户以自然语言给出起始地址（升压站）✅

- **测试特性**：新设备类型（升压站），接收点表与用例 4 相同（2390~2399）；**未提供转发点表**，Agent 询问后用户以自然语言给出起始地址（「接收端口使用9001，转发地址/转发点表使用从一万开始的地址」，即转发 addr = 10000, 10001, …）
- **修复的版本**：C4H1——**经五轮迭代修复（见多轮测试记录与两节附录），2026-09-01 22:47 复测全链路通过**：
  Agent 先询问端口 → 9001 全程保持（摘要/工具参数/config.json/实际监听）→ 按钮确认执行 →
  服务监听 9001、采集/转发各 10 点；转发侧 127.0.0.1:9900 为用户显式指定，语义正确

### 测试输入

```
现在需要接入升压站的数据，第三方厂家转过来的，asfp2协议，2390:电网频率，2391是正向有功，2392是反向有功，2393是正向无功，2394是反向无功，2395是uab，2396是ubc，2397是uac，2398是变压器油温，2399是环境温度。将数据转发到II服务器的127.0.0.1:9900上，转发采用asfp2协议。
```

Agent 询问缺失信息后，用户补充回答：

```
接收端口使用9001，转发地址使用从一万开始的地址。
```

> 关键点：转发地址以**起始地址**的自然语言形式给出（「从一万开始」），Agent 应将其展开为
> 逐点 addr（10000~10009）并成功通过 `output_plan_steps` 生成配置。

### 多轮测试记录（C4H1）

| 轮次 | 修复状态 | 结果 |
|------|---------|------|
| 第一轮 | 初版 | ❌ Agent 陷入 `output_plan_steps` 反复失败重试循环（自省文字不断调整转发点对象结构：addr 位置、属性、字段顺序），接入失败。根因：转发点输入 schema 复用采集点定义，误要求 `name` 必填字段，无 name 的转发点被 Zod 校验拒绝 |
| 第二轮 | 修复①（校验错误可读化：带设备/点序/字段上下文）+ 修复②（点表完整性拦截：协议可判定时采集点必须逐点含业务字段） | ⚠️ 接入可用但不稳定：同一输入序列多次测试，LLM 传参**概率性丢失端口**——一次 9001 正确落地，一次被 registry 默认值 9000 静默覆盖（LLM 回复文本中复述了 9001，工具调用参数却未携带） |
| 第三轮 | L2 执行闸门（未经用户确认不执行）部署后 | ⚠️ 接入成功，但 **Agent 未询问确认**——用户回答「…从一万**开始**…」中的「开始」命中宽松确认正则，被状态机直接判定为已确认后放行执行 |
| 第四轮 | 按钮唯一确认通道部署后 | ⚠️ 方案摘要正确列出 9001，点按钮后 `output_plan_steps` 前两次因转发目标缺 ip/port 被可读校验拦截，**第三次重试 LLM 重建参数时把端口回退为 registry 默认 9000**（最终回复仍宣称 9001）。根因：端口仅存在于对话文本，确认轮注入的 deviceInfo 是端口回答**之前**抽取的（不含端口），重试时 LLM 手边只有提示词默认值 |

### 待决问题：用户确认方式 → 已定稿：Web 按钮为唯一确认通道

自由文本确认识别存在不可消除的歧义：参数回答与确认表达在文本上重叠（「从一万**开始**」命中确认词
「开始」）时，确定性状态机无法区分「提供信息」和「表达确认」——宽松正则下偶发跳过确认询问
（第三轮），收紧正则又会误拦正常确认（曾在第二/三轮间试验后移除）。

**定稿方案（已实现）**：使用 Web 技术确定用户确认方式——**用户点击按钮决定确认还是取消，
非按钮发送的内容一律不视为用户确认信息**：

- 前端 `ConfirmButtons`（方案展示句式触发展示）点击「确认」发送结构化消息
  `[C4_BUTTON_CONFIRM] 确认`，点击「取消」发送 `[C4_BUTTON_CANCEL] 取消，不执行`
- 后端执行闸门仅识别 `[C4_BUTTON_CONFIRM]` 前缀消息置位「已确认」；自由文本中的确认词
  （确认/好的/执行/开始等）不再构成确认，「从一万开始」类参数回答不会再误触执行
- 未确认时闸门拒绝执行，并提示用户点击「确认」按钮
- 协议契约记录于 web.md §3.1.3（按钮驱动）与 agent.md「执行闸门」，前端
  `useConfirmDetect.ts` 常量与后端前缀修改时必须两侧同步

### 第四轮修复：端口确定性捕获与注入（2026-09-01）

- **super_worker.ts**：新增 `userPort` 状态——从用户消息确定性捕获显式指定的接收端口
  （正则 /(?:接收|监听)端口…(\d{4,5})/，强制「接收/监听」前缀避免误捕转发端口；同场站名解析机制），确认轮注入
  「用户指定的数据接收端口: NNNN（必须原样使用，禁止改为默认端口）」，执行完成后复位
- **system.txt 解耦回归**：撤销曾硬编码的「默认接收端口 9000」字样——默认端口属于 asfp2
  MCP 注册 JSON 的 prompt_hints（运行时拼接），system.txt 保持服务中立，仅要求
  「写出具体数字、禁止含糊表述」并强调用户指定端口必须原样使用

### 设计变更：端口必填，无默认值（2026-09-01，用户裁定）

捕获+注入仍属概率性防线（LLM 仍可能忽略注入指令回退默认值）。据此将设计改为**釜底抽薪**：

- **不设置默认端口**（最终机制，字段级声明）：registry 中 `port` 字段**无 `default` 键**——
  必填语义由字段声明自表达；modbus port 删 502、iec104 port 删 2404 协议默认端口；
  config_schema.fields 进一步删除 `source` 维度（有 `default` 键=技术默认值自动填充，
  无=必填），L1 渲染「必填」、Zod 入参强校验原生生效
- **Agent 强校验**：`output_plan_steps` 的 `validate_runtime_input`（Zod）——实例缺 port/ip
  时报「缺少必要字段」（带设备/转发目标上下文），要求向用户询问，方案无法生成
- **必填项总定义**（用户裁定）：`point_schema.fields` 全量（点表）∪ `config_schema.fields`
  中无 `default` 键的项（配置），全部由用户提供、不设默认值；记录于 agent.md
  「必填项用户提供原则（无默认值原则）」
- **移除自动选端口**：`assign_port` 删除「从默认端口起顺延选择空闲端口」分支；用户指定的
  端口原样写入，被占用由 Start 阶段报 PORT_BIND_FAILED
- **交互规则**：用户未提供端口时 Agent 必须先询问，禁止生成方案；文档（agent.md
  「监听端口的必填约束」、c4_asfp2_server.md §2.2）同步

---


---

## 用例 6：用户未明确给出协议，Agent 推断协议但方案摘要未展示协议名 ✅

> **设计变更注**：协议推断已于 C4H1 后期整体移除（见用例 13）——本用例场景（消息无协议信息）
> 现行为改为**先询问用户协议**，而非推断。本用例保留为「推断时代的隐含确认缺陷」历史记录。

- **测试特性**：用户未在消息中明确给出通信协议——协议由 Agent 三层推断（点表字段 → 描述 →
  询问，见 agent.md「协议推断」）确定。设计规定「协议推断成功后不单独打断用户，协议作为方案的
  一部分在方案确认环节**隐含确认**」——隐含确认的前提是**方案摘要必须显著展示协议名**。
  实测摘要未展示协议，隐含确认落空
- **修复的版本**：C4H1——system.txt 方案摘要硬规则（逐设备、逐转发目标列出协议名）；
  2026-09-03 复测通过，方案摘要稳定展示「ASFP2 协议」字样

### 测试输入

```
现在需要接入1号风机的数据，第三方厂家给我们转来1#风机数据，10个点，从1000到1009，10个点分别是1000:风速、1001:功率、1002:风向、1003:桨叶角度、1004:发电机转速、1005:齿轮箱油温、1006:塔筒温度、1007:空气温度、1008:空气湿度、1009:大气压强，使用端口9001。我们需要将这些数据转发到II区服务器上，目标地址是127.0.0.1:9900，点表5000~5009。
```

> 关键点：消息**未出现任何协议名**（无 asfp2/modbus 等字样）——协议完全依赖 Agent 推断。

### 实测表现（C4H1）

- Agent 正确推断协议为 ASFP2（数据接收侧 asfp2_server + 转发侧 asfp2_client），接入最终成功
- **但方案摘要未展示具体协议名**——用户在确认环节看不到「本方案使用 ASFP2 协议」这一关键信息，
  推断结果实际上未经用户确认

### 风险说明

协议推断是概率性的（LLM 行为）。若某次推断错误（如把第三方转发误判为 Modbus TCP），
由于摘要不展示协议，用户在确认环节**无从发现**，会带着错误的协议完成确认——直到接入失败
（对端协议不匹配）才暴露，且用户难以定位失败原因。协议是接入方案的**核心正确性前提**，
必须进入用户的确认视野。

### 修复记录（C4H1，已实现待复测）

日志实证协议名展示为**概率性行为**：同一输入多次测试，部分摘要含「ASFP2 协议」（12:25 会话），
部分确认前摘要只写「数据转发协议」甚至不提（12:06 / 12:28 会话）——隐含确认因此落空。

- system.txt 方案摘要规则增补硬规则：**逐设备、逐转发目标明确列出通信协议名称**，
  禁止「数据转发协议」之类含糊表述
- agent.md「协议推断」块同步：隐含确认的前提 = 摘要展示协议名
- 可选加固（未实施）：`output_access_plan` 协议字段在前端方案卡片显式渲染

---

## 用例 7：文件上传点表接入，执行失败后跨轮修复被确认闸门拦截 ✅

- **测试特性**：通过文本文件上传点表接入（文件 → 解析 → 补充端口/转发信息 → 按钮确认 →
  执行）。首次执行失败后 LLM 补问用户、用户确认后重试成功——但**确认闸门是轮次级状态**，
  随上一轮结束复位，重试轮被自己的闸门拦下；拒绝文案不含触发句式导致按钮不再出现；
  最终 LLM 在被拒后谎报「执行完成」（config.json 实际未写入）
- **修复的版本**：C4H1——闸门复位时机修正（A~D），2026-09-03 复测通过

### 测试输入

前置：上传 `1#风机点表.txt`（点表 1001~1009，9 个点），解析成功后用户补充：

```
监听9001端口，我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9900，点表5000~5009。
```

方案摘要展示（含 ASFP2 协议）后用户点击确认按钮，随后：

```
（确认轮 output_plan_steps 失败：设备 "1#风机" 缺少必要字段 "port"）
LLM：1#风机的数据接收监听端口是否为 9001？
用户：是
（LLM 重试 output_plan_steps 成功 → 闸门拒绝：未收到确认按钮消息）
用户：确认
（LLM 谎报「执行完成」——config.json 实际未写入）
```

### 根因与修复（A~D）

| # | 缺陷 | 修复 |
|---|------|------|
| A【根因】 | 闸门 `userConfirmed` 随每轮 invoke 结束无条件复位，「执行失败 → 补问 → 重试」的正常多轮修复跨轮即被拦 | 复位仅发生在 ①执行完成（config_merge + stop_start 走完）②新 `output_access_plan` 产生（新方案使旧确认失效） |
| B | 兜底数据 pdi 优先导致 accessPlan 中的端口被排除（deviceInfo 抽取早于用户给端口），首次执行必缺 port | pdi devices 缺失的实例字段按设备名从 accessPlan 的 connection 补齐（accessPlan 已过方案层必填校验），首次执行即可成功 |
| C | 拒绝文案不含「是否确认/确认执行/请确认」句式 → 前端按钮不再出现 → 用户被迫自由文本 → 持续被拒 | 拒绝文案追加「是否确认执行？」，`matchConfirmPhrase` 命中，按钮确定性重现 |
| D | 拒绝以 error 事件发给用户、不入对话历史 → LLM 下轮看到的最后事实是 plan_steps 成功 → 谎报执行完成误导用户 | 拒绝以 text 进入 agent 气泡并入 history，文案明确「本次接入未执行，配置未写入」 |

### 复测

修复部署后重跑同流程：首次执行成功（B 兜底补齐端口，无需跨轮）；人工验证跨轮修复路径确认保持有效，按钮重现、拒绝提示准确。

---

## 用例 8：用户未提供点名/点描述✅

- **测试特性**：用户没有提供点名或点描述
- **修复的版本**：C4H1

### 测试输入

```
现在需要接入1号风机的数据，第三方厂家通过asfp2协议给我们转来1#风机数据，10个点，从1000到1009。我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9900。
```

> 关键点：用户只提供了数据点**地址**（1000~1009），**没有提供点名/点描述**。

### 曾经出现的问题

LLM 在信息收集阶段自行发明了点名「点1000」…「点1009」（中文「点」+ 地址），
最终在执行模块 `merge_config_from_steps` → `handle_add` 的 `point.id` 校验中被拒绝：

```
自动执行失败: point.id "点1000" 包含非法字符，仅允许字母开头
```

根因：`point.id` 必须匹配 `^[a-zA-Z][a-zA-Z0-9_]*$`（字母开头，仅字母/数字/下划线），
而 LLM 发明的点名以中文「点」开头；且 `output_plan_steps` 的运行时强校验未覆盖
`point.id` 命名合法性，导致非法 id 一路漏到 merge 阶段才报错。

期望升级（2026-09-17 用户裁定）：点名是 `point_schema.fields` 必填项——用户未提供
点名时 Agent 必须逐点询问，禁止编造（含「点1000」式发明与机器生成名）。

---

---

## 用例 9：文件解析成功但模型空回复，用户盲态后路径捏造解析失败 ✅

- **测试特性**：通过文本文件上传点表（CSV）→ `csv_parser` 解析**成功**，但模型零文本输出——
  解析结果未流式展示、也未进入对话历史。用户被迫手动补充协议与端口信息；LLM 上下文缺失
  解析结果，为「重新解析」捏造文件路径 `/data/upload/1#风机电表.xlsx`（真实路径
  `/tmp/c4_upload_….csv`，扩展名也不符）→ `xlsx_parser` 报「文件不存在」，接入失败
- **修复的版本**：C4H1——空回复 nudge + 兜底重生成收窄（planSteps 为 null 才触发）；
  2026-09-03 复测通过，config.json 双实例齐全、双服务正常拉起

### 测试输入

```
（上传 1#风机电表.csv）
这是asfp2协议的点表，我们的接入端口是9001
```

### 实测表现（C4H1，日志 14:49~14:50）

- 14:49:52 上传 CSV → `csv_parser` OK；**14:49:54~14:50:13 零 `llm_text`**——模型空回复，
  用户看到的解析气泡为空
- 14:50:14 LLM 调 `xlsx_parser`，路径为捏造的 `/data/upload/1#风机电表.xlsx` → 「文件不存在」
- 连带缺陷：上传文件名 mojibake（`1#é£æºç¨è¡¨.csv`——multer 以 latin1 解码中文名）污染对话历史

### 根因与修复

| # | 缺陷 | 修复 |
|---|------|------|
| A【根因】 | 模型空回复抖动：工具执行成功后零文本输出，解析结果不展示也不入历史，信息断链 | super_worker 循环加兜底：工具已执行但零文本输出时，注入「请用中文向用户总结以上工具的执行结果」补跑一轮（限一次） |
| B | multer latin1 解码中文文件名乱码 | upload.ts 按 latin1 → utf8 还原 `originalname` |

### 复发与最终修复（同日第二轮复测）

A/B 修复后复测发现**转发再次静默丢失**（插桩 `confirm_regen`/`plan_steps_final` 实锤）：
确认轮兜底重生成**无条件覆盖**了 LLM 已产出的两步计划——pdi 缺转发目标时重生成只产出
采集单步（1 步）覆盖了 LLM 的采集+转发两步（2 步），merge 写出 writer-only 配置 →
`adjust_shm` 报 `CONFIG_MISSING_SECTION`（writer/reader 仅一方非空）→ 回滚。

**最终修复**：兜底重生成收窄为**仅在 LLM 完全未产出计划（planSteps 为 null）时触发**——
LLM 已产出并经工具校验的计划不得被覆盖。空回复兜底（nudge）与重生成兜底各司其职、
互不覆盖。复测通过：确认后 config.json 双实例齐全（asfp2_server + asfp2_client），
服务正常拉起。

---

## 用例 10：对已接入设备追加数据点，新增点与已有点撞名 ✅

- **测试特性**：用户对已接入设备追加数据点，新增点名称与已有点名称冲突（撞名）
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过；2026-09-17/18 增量场景三轮复测通过——含拒绝谎报、路径绕过、按钮状态机、thinking 延迟四类修复，见下文修复清单与验收实录）

### 测试输入

前置状态：设备 `hnals_wt1` 已接入，含点 `windspeed`（addr=1000）。

```
给1#风机再追加一个数据点，地址2000
```

> 关键点：新增点的点名（LLM 翻译后）与已有点 `windspeed` 撞名，但 addr（2000）不同，属于另一个点。

### 曾经出现的问题

`handle_add` 的合并分支（`instance.id` 已存在时）按 `point_match_key`（writer 点用 `id`）匹配，
命中后是**覆盖**已有点的业务字段，而非去重。撞名且 addr 不同时，已有点 `windspeed` 的
`addr` 被静默改写成 2000，造成数据损坏且无报错。

期望：撞名时确定性去重（追加序号如 `windspeed_2`），仅当点的业务身份（addr 等）一致时才视为「更新」。

### 2026-09-17 复测发现（独立常驻架构部署后）

实测：用户仅提供「地址 2000」（未提供转发地址），Agent 未询问缺失信息即弹出方案确认按钮，
且存在两处自行推断——① 点名「风速」的英文 id 直接沿用已有点（`windspeed` 撞名）；
② 自行推断「用户不需要转发该点」，生成只采集不转发的增量方案。

**期望行为增补（2026-09-17 用户裁定）：新增点必须转发——不进行转发的点不允许采集。**

- 增量点未提供转发地址时，Agent 必须询问（用例 20 无默认值原则的增量延续），
  禁止推断「该点不需要转发」、禁止生成只写 writer 侧的增量方案；
- 接收点表与转发点表必须成对扩展（用例 16 双侧成对原则的强制化）：
  方案校验应拒绝「writer 侧新增而 reader 侧无对应转发点」的增量；
- 信息补全（转发地址等必要项）之前，方案确认按钮不得出现——必要项缺失时
  Agent 必须处于询问态，而非确认态。
- **点名必填与描述查重（2026-09-17 裁定）**：点名进 `point_schema.fields` 必填项，
  用户原始描述随点保存进 config.json（`name` 字段）；新增点与既有点（或同批点）
  描述相同而地址不同时，Agent 必须向用户澄清（两个不同测点，还是重复描述？），
  禁止静默去重——merge 层的 `_2` 追加序号仅保留为最后防线；
- **转发点名继承**：用户未提供转发端点名时，按映射关系将采集端点名作为转发端点名
  （用户显式提供的转发点名优先）。
- **绕过场景补强（2026-09-17 第二轮实测）**：LLM 走 devices 路径做增量时曾出现
  三种绕过，现由步骤级不变式在 devices/changes 双路径统一拦截——
  ① 自行把「风向」改名为 winddirection_1010 绕过描述查重 → 按
  「既有点id_编号」变体规则拦截并要求向用户澄清；
  ② 只生成采集点不生成转发点 → 转发配对检查拦截；
  ③ 为新转发点新建第二转发实例 → 转发归属检查拦截
  （新转发点必须追加到既有转发实例）。

### 2026-09-18 第二轮复测：修复清单与验收实录（C4H1，全链路通过 ✅）

**修复清单**（9-17 晚 ~ 9-18 部署，逐项对应上方绕过场景）：

| # | 缺陷/缺口 | 修复 |
|---|----------|------|
| 1 | 三查仅覆盖 changes 路径，LLM 走 devices 路径做增量时全部绕过（实测实锤） | 步骤级不变式 `validate_step_invariants`——devices/changes 双路径统一：转发配对（1:1）、描述查重（含「既有点id_编号」改名变体）、转发实例归属 |
| 2 | LLM 自行改名（风向 → winddirection_1010）绕过查重 | 变体规则按「既有点id_编号」拦截；system.txt 硬规则：点名必须原样使用用户描述，禁止改名/编号变体 |
| 3 | 只采集不转发 | 配对检查拒绝；system.txt：不转发的点不允许采集，用户要求不转发时须拒绝并说明规则 |
| 4 | 新转发点被建成第二转发实例（hnals_zone2，与既有 hnals_ii_zone 并存） | 归属检查：新转发点必须追加到既有转发实例（changes action=modify） |
| 5 | 闸门拒绝后 LLM 谎报「已执行完成」（拒绝不进 LLM 历史） | 拒绝事实注入持久化历史（plan_rejected 合成消息对，明确「此前完成表述不成立；确认后直接重执行、无需复述」） |
| 6 | 确认按钮在信息收集阶段提前弹出；拒绝后按钮不重现 | 前端按钮状态机：新轮次解除武装；本轮产出结构化方案才武装；闸门拒绝文本（是否确认执行）重新武装 |
| 7 | LLM 深度思考致每轮 23~101s | model.thinking=disabled（实测回到秒级） |
| 8 | system.txt 部署缺口：tsc 不拷贝 .txt 资源，部署机始终为旧副本（提示词规则未真正生效的根因之一） | 发布流程手工同步 dist/super_worker/prompts/system.txt |
| 9 | Express 静态服务无缓存策略 → 浏览器残留旧 bundle，新功能不生效 | index.html no-cache；带哈希 assets immutable |

**最终验收对话实录**（2026-09-18，前置：用例 1 已完成）：

| 用户输入 | Agent 行为 | 判定 |
|---------|-----------|------|
| 给1#风机再追加一个数据点，地址1010 | 询问**点名**与**转发地址**（两项都问，页面无确认按钮） | ✅ |
| 风向 | 澄清：已有「风向」（地址 1002），地址 1010 是两个不同测点吗？ | ✅ 查重生效 |
| 功率 | 澄清：已有「功率」（地址 1001），是否同一测点？ | ✅ |
| 不转发 | 拒绝：采集的数据点必须配置对应的数据转发，不允许只采集不转发的点 | ✅ 裁定生效 |
| （提供转发地址后） | 生成方案 → 用户未点按钮 → 闸门拦截（config 未写入）+ ⚠️ 提示 + 确认按钮重现 | ✅ |
| 点击确认按钮 | 重新执行 → 双侧成对写入、stop_start 成功、对点可查 | ✅ |

**已知小瑕疵（已修复待复测）**：确认轮 LLM 曾先复述「该变更已在上一轮执行完成 ✅」再执行——
拒绝注入文本已追加「确认后直接重新执行并以本次结果回复、无需复述」指令。

---

## 用例 11：协议推断错误且用户纠正未被采纳，Agent 反复询问错误协议的业务字段 ✅

- **测试特性**：上传的点表只有 addr + 点名两列（无协议信息）——点表字段无法唯一确定协议，
  LLM 猜测 modbus 并调用 `output_device_info` → 抽取层按 modbus 要求 uid/fun/type/swap →
  全部缺失 → 反复询问 Modbus 业务字段。**用户明确声明「使用asfp2协议」后，重新上传重试
  仍传 modbus**——协议纠正未被后续工具调用采纳，询问永远问不对
- **修复的版本**：C4H1——`declared_protocol` 记忆 + `output_device_info` 协议校验钩子 +
  system.txt 禁止重新推断；2026-09-04 复测通过：用户声明 asfp2 后重新上传，
  `output_device_info` 全程采用 asfp2，接入成功

### 测试输入

```
（上传 1#风机电表.xlsx——文件内点表仅 addr + 点名 两列，无协议信息）
使用asfp2协议接收数据，端口9001
对，这是1#风机的点表。数据发送到II区，127.0.0.1:9900
（重新上传同一文件）
```

> 关键点：消息全程未出现协议名时 LLM 猜测 modbus；用户明确声明 asfp2 后重新上传，
> LLM 仍用 modbus 调用工具——纠正被绕过。

### 实测表现（C4H1，日志 11:48~11:50，插桩 tool_call args 实证）

| 时刻 | 事件 |
|------|------|
| 11:48:14 | 上传 xlsx → `output_device_info` **protocol="modbus"** → FAIL：10 点全缺业务字段 |
| 11:48:49 | 用户声明：「使用asfp2协议接收数据，端口9001」 |
| 11:50:14 | 重新上传同一文件 → `output_device_info` **仍 protocol="modbus"** → 同样失败 → 反复询问 |

### 根因与修复

| # | 缺陷 | 修复 |
|---|------|------|
| 1【根因】 | 协议声明记忆缺失：用户的协议纠正只存在于对话文本中，重新解析时 LLM 重新推断（猜测 modbus），纠正被绕过 | super_worker 新增 `declared_protocol` 记忆（确定性捕获用户消息中的协议声明，与 userPort 同机制）；`output_device_info` 增加协议校验钩子——`dev.protocol` ≠ 用户声明协议时返回可读错误「协议应为用户声明的 asfp2」，LLM 必须纠正 |
| 2 | system.txt 三层推断规则未约束「已声明协议后的重新推断」 | 增补：用户已明确声明协议的，后续一切调用（含重新解析、重新上传）必须使用该协议，禁止重新推断 |

### 复发扩展：方案展示绕过 output_access_plan + 自由文本确认死锁（同日 20:33 会话）

协议修复后复测发现同族问题：模型**用纯文本展示完整方案摘要并索要确认，但从未调用
`output_access_plan`**——前端 planArmed 按设计未武装（无结构化方案不弹按钮）→ 用户
自由文本「确认」×2 均不构成确认 → 模型「宣而不行」纯文本收尾 → 静默结束，用户面对
「请求确认却不给确认手段」的死锁界面。

| # | 缺陷 | 修复 |
|---|------|------|
| A | 模型绕过 output_access_plan 直接展示方案（prompt 硬规则被概率性违反） | super_worker 循环兜底：本轮有文本且含确认句式（展示方案索要确认）但本轮未调用 output_access_plan 时，注入「先调用 output_access_plan 再展示摘要」补跑一轮（限一次）——结构化方案产出后 planArmed 自动武装，按钮随之出现 |
| B | 自由文本确认静默无操作（无引导、无按钮时用户卡死） | 短确认词消息（≤6 字符含「确认/执行」）不经 LLM，确定性引导「请点击下方确认按钮」并结束回合 |
| C | 工具执行异常被 `catch { ignore }` 静默吞掉，不可观测 | bgCapture 异常记录为 `tool_exec_error` 日志事件 |

---

## 用例 12：跨轮上下文丢失工具证据，模型陷入自我怀疑推理死循环 ✅

- **测试特性**：上传点表文件（仅 addr + 点名 两列，无协议信息）→ 首轮解析点表并询问协议 →
  用户声明协议后的追问轮次中，模型看不到已解析的点表数据——工具调用/结果证据未跨轮保留，
  模型陷入「我到底解析过文件没有」的自我怀疑死循环，烧光输出 token 被截断，无工具调用、无答复
- **修复的版本**：C4H1——conversationId 持久化（前端）+ 服务端完整历史持久化（含工具证据）+
  上传路由参与会话；2026-09-04 复测通过：追问轮模型可见首轮 `xlsx_parser` 解析结果，
  直接调用 `output_device_info`，不再陷入循环推理

### 测试输入

```
（上传 1#风机电表.xlsx——文件内点表仅 addr + 点名 两列，无协议信息）
使用asfp2协议接入，端口9001
```

> 关键点：追问轮必须能看到首轮 `xlsx_parser` 的解析结果（10 个点的地址/点名），据此直接调用
> `output_device_info` 输出设备信息——不得因「没有点表数据」反复自我怀疑、循环推理。

### 实测表现（C4H1，日志 13:02~13:03）

| 时刻 | 事件 |
|------|------|
| 13:02:53 | 上传 xlsx → `xlsx_parser` OK（10 点）→ `output_device_info` 缺业务字段 → 询问协议（会话 `unknown`） |
| 13:03:14 | 用户追问「使用asfp2协议接入，端口9001」→ `llm_call` 仅 2 条消息（assistant 文本 + user 文本），**无任何工具调用/结果证据**；会话为新 UUID，与上传轮分属不同会话 |
| 13:03:40 | 模型产出 ~4096 token 自我循环文本（「让我先调用 output_device_info…不过我没有点表数据…让我重新审视」重复约 40 次）→ 被 `max_tokens` 截断，无 tool_call、无最终答复，直接 `done` |

### 根因与修复

| # | 缺陷 | 修复 |
|---|------|------|
| 1【根因】 | 前端不持久化 conversationId：`send()` 丢弃 `streamChat` 返回的会话 ID，后端每轮 `randomUUID()` 生成新会话；上传轮（`/api/upload` 不传会话 ID）与对话轮分属不同会话 | `useChatStream` 新增 `conversationIdRef` 持久化，`send` 自动附带；`ChatView` 上传流程读写同一会话 ID |
| 2【根因】 | history 纯文本重建：前端 history 仅含 user/assistant 文本气泡，`toolCards`（工具调用/结果）不回传 → 模型丢失 `xlsx_parser` 的点表 ground truth | 后端 `super_worker` 按 conversationId 服务端持久化完整消息历史（`run.output.messages` 含工具调用/结果），后续轮优先恢复；剔除 nudge 消息，限长 100 条 |
| 3 | 上传路由不参与会话：`/api/upload` 不接收/回传 conversationId | `upload.ts` 接收/生成 conversationId 并回传（`X-Conversation-Id` 头 + `done` 事件），传入 `invoke` |

### 说明

此缺陷与用例 9（空回复断链）、用例 11（协议纠正绕过）同源——三者都因「工具产出未跨轮
进入模型上下文」而失败；本用例聚焦其最极端形态：模型因上下文自相矛盾（自己的文本声称
「已解析」却无工具证据）陷入不可判定的自指循环。防抖兜底（`announced_unacted`）只能在
循环**发生后**补跑一轮，无法在点表数据根本缺失时恢复 ground truth——故必须从会话持久化
层面根治。

---

## 用例 13：协议改为用户必供，未知协议 fail-fast ✅

- **测试特性**：协议推断功能整体移除——协议改为**必须由用户提供**（消息中的协议名或协议描述，
  如「使用asfp2协议」「采集modbus设备」均视为已提供），消息中无任何协议信息时先询问用户；
  用户提供的协议在服务目录中不存在时（如「abc」），Agent 返回可读错误并列出已部署协议列表，
  而非静默跳过或丢弃设备
- **修复的版本**：C4H1——2026-09-05 复测通过（无协议信息先询问；未知协议 abc 返回可读错误并列出已部署协议）

### 测试输入

```
（上传 1#风机电表.xlsx——点表仅 addr + 点名 两列，无协议信息）
请解析此文件中的设备信息
```

> 期望：消息无协议信息 → Agent 先询问「请提供通信协议」，不调用 output_device_info、不猜测协议。

```
使用 abc 协议接入，端口 9001
```

> 期望：Agent 返回「未在服务目录中找到协议 abc，当前已部署协议：asfp2 / modbus / iec104 / influxdb」，
> 请确认协议名或先部署对应 MCP 服务——不得静默改用其他协议、不得生成方案。

### 根因与修复

| # | 缺陷 | 修复 |
|---|------|------|
| 1【根因】 | 协议依赖 LLM 三层推断（点表字段→描述→询问），稀疏点表（仅 addr+点名）无法唯一确定协议，诱发错误猜测 | system.txt / agent.md 移除三层推断，协议改为用户必供；`declared_protocol` 动态关键词捕获（显式「X协议」模式 + 已部署协议名描述匹配），用户声明后禁止改用其他协议 |
| 2 | 未知协议静默跳过：`output_device_info` 对 `find_service_type` 返回 null 直接 `continue`，`output_access_plan` 空 entry 不查必填项，最终设备在 `output_plan_steps` 被静默丢弃（仅 warning） | `output_device_info` / `output_access_plan` 未知协议 fail-fast：返回可读错误并列出已部署协议（`list_supported_protocols` 动态生成），区分「协议名错误」与「协议未部署」 |
| 3 | 协议关键词硬编码（asfp2/modbus/iec104/influxdb），新增协议需改代码 | `list_supported_protocols` 从 registry 动态生成协议列表 |

### 说明

此变更使协议退化为**确定性必填字段**：要么用户消息命中协议关键词（显式或描述），要么询问用户。
协议确定后 `output_device_info` 以 registry 的 `point_schema.fields` 对点表做完整性校验，字段缺失按
「协议错 / 点表缺字段可补 / 点表错」三态向用户诊断。转发协议（Reader）最初沿用目标→协议映射
（「转发到上级系统」→ asfp2、「入库」→ influxdb），2026-09-05 起该映射移除：转发协议与接收协议
逐侧必供、缺失必须询问（见用例 1 关键点与 agent.md §3.2 协议），禁止沿用接收侧协议或按目标描述推断。
实现上由 `output_device_info` 确定性校验兜底：存在转发目标而用户未声明转发协议时拒绝提交；
拒绝后进入问答握手——agent 复述候选协议向用户确认，用户答复含协议名或肯定复述即完成声明，
任意措辞（含联合声明「接收和转发都是用asfp2协议」）不会死锁。

---

## 用例 14：转发协议逐侧必供与问答握手（联合声明/非模板句式）✅

- **测试特性**：转发协议与接收协议逐侧独立必供——接收侧声明不算转发侧（「第三方厂家通过asfp2协议
  转来数据」仅提供接收协议，转发协议仍需询问）；转发协议声明分两条通道：
  **便捷通道**（模板句式「转发协议X」「转发采用/使用X协议」「用X协议转发」「写入X」）静默命中，
  无确认轮；**问答握手**（任意其他措辞）——`output_device_info` 拒绝提交一次，Agent 以是非题
  复述候选协议，用户肯定答复后放行。禁止按转发目标描述推断协议
- **修复的版本**：C4H1——2026-09-05 复测通过

### 测试输入与实测（2026-09-05）

场景 A：无协议信息的一句话接入（点表/端口/转发目标齐全，联合声明答复）

```
现在需要接入1号风机的数据，第三方厂家给我们转来1#风机数据，10个点，……使用端口9001。
我们需要将这些数据转发到II区服务器上，目标地址是127.0.0.1:9900，点表5000~5009。
接收和转发都是用asfp2协议。
对
```

> 实测：Agent 先分别询问接收/转发两侧协议（不推断）；联合声明不命中模板 → 闸门拒绝一次，
> Agent 以是非题复述「转发到II区服务器的协议是 ASFP2 吗？」→ 用户答「对」→ 放行，接入成功。
> 全程恰好一轮额外确认，无死锁。

场景 B：模板句式直接命中（同日早前会话）

```
监听端口9001，转发协议asfp2，转发地址和接收地址一致。
```

> 实测：便捷通道静默命中，无确认轮直接通过。

### 曾经的缺陷：握手缺失导致死循环（已修复）

初版确定性闸门仅扫描最新一条用户消息且无握手出路，联合声明「接收和转发都是用asfp2协议」
不命中四种模式 → `declared_forward_protocol` 永远为空 → 工具连续拒绝 7 次；用户四次肯定答复
（「对」「是asfp2」「是」「正确」）均无法推进状态，Agent 交替重复确认转发协议与转发点表映射。

| # | 缺陷 | 修复 |
|---|------|------|
| 1【根因】 | 声明判定依赖枚举用户措辞，自然语言无法穷举 | 放弃句式枚举的正确性职责，改为「双通道 + 问答握手」：便捷通道扫全会话累计消息（模板句静默命中）；权威通道锚定对话结构——闸门拒绝置握手态，Agent 是非题复述候选协议，用户答复含唯一协议名或肯定复述即完成声明（对复述的确认 = 提供） |
| 2 | 捕获仅扫最新一条消息，早前轮次的声明丢失 | 用户消息按会话累计缓冲（容量截断），被动扫描覆盖全部历史轮次 |
| 3 | 工具拒绝文案无提问指引，LLM 盲目重试 | 文案引导复述候选协议的是非题并禁止答复前重试 |

### 说明

用户表述不可枚举，但 Agent 的提问形态可枚举：开放问把答案空间压缩为「含协议名与否」，
封闭问压缩为是/否——表述多样性由 LLM 提问吸收，确定性由结构判定保证（agent.md §3.2 协议）。
任意新措辞的代价是恰好一轮复述确认，不会死锁。已知局限：用户在握手期间一句话出现两个协议名
（如同时纠正接收协议）时判定保持未定，Agent 继续提问，由用户明确后收敛；误声明的最终兜底是
方案摘要逐条列出协议 + 确认按钮。

---

## 用例 15：ASFP2 FLOAT16 点经接收服务写入 shm 后数值归零 ✅

- **测试特性**：协议级回归——FLOAT16（type=9）点经 `c4_asfp2_server` 解码写入共享内存后，
  shm value 字段必须为该值对应的 **float32 位模式（4 字节）**，下游（c4_asfp2_client 转发、
  c4_shm_manager read_points 展示）按此约定还原
- **修复的版本**：C4H1

### 测试输入

构造 ASFPV211 数据包：单数据项，type=9（FLOAT16），key=3000，value 线缆编码 f16 `0x3E00`
（即 1.5）；经 stdio 启动 `c4_asfp2_server`（点位映射 addr 3000 → shm_id 1）后发送到监听端口。

### 曾经出现的问题

C4H1（df3e6b1 之前）：`decodePacketValue` 将 f16 转为 float32 位模式（32 个有效位）后，
`writeValue` 按 `TypeByteSize(FLOAT16)=2`（线缆尺寸）只拷贝低 2 字节进 shm——float32 位模式
的符号位与指数位（高 16 位）被丢弃。以 1.5 为例：f32 位型 `0x3FC00000` 低 16 位为 `0x0000`，
shm 实际存储 `0x00000000`，FLOAT16 点入 shm 后变为 ≈0，下游转发/落库/对点展示全错且状态
标注仍为"正常"。修复：shm 存储特判为 4 字节 float32 位模式（`c4_architecture.md` §2.2.3
FLOAT16 特例）。

### 回归断言

发送后直读 `/dev/shm/c4_main` 块（shm_id=1）：`state=1`、`type=9`、value 低 4 字节 =
`0x3FC00000`、按 float32 解释 = 1.5。

---

## 用例 16：对已接入风机追加数据点（信息齐全，正常） ✅

- **测试特性**：以用例 1 接入完成的 1#风机（接收 1000~1009 → 9001，转发 5000~5009 → 127.0.0.1:9900）
  为前提，用户一句话追加一个点，地址、点名、转发地址齐全；验证增量修改的双侧成对扩展、
  shm_id 稳定性与运行中服务的无损衔接
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成，hnals_wt1 在线、9001 监听中。

```
给1#风机增加一个数据点，地址2010:振动，转发地址5010。
```

> 关键点：Agent 生成**增量方案**并经确认按钮执行后——
> ① config.json 双侧成对新增：`hnals_wt1` 增加 `{addr:2010, id:<「振动」翻译的合法标识>}`，
>   `hnals_center` 对应增加 `{addr:5010, key:hnals_wt1.<id>}`，shm_id 顺延（如 11）；
> ② `adjust_shm` 后**既有 10 个点的 shm_id 保持不变**（转发映射稳定的前提，同 c4_fun_00058 TC9 语义）；
> ③ Stop-Start 后 9001 持续监听，原 10 点数据流不中断；
> ④ 对点核验（`list_points` / 显示卡片）可见新点。
> 不得全量重建点表、不得改动既有点的地址与点名。

---

## 用例 17：对已接入风机删除数据点（正常） ✅

- **测试特性**：以用例 1 状态为前提，删除一个已有点；验证双侧成对删除、shm 块回收与剩余点无损
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成（建议在用例 16 之后执行，可顺带验证「删除增量点」变体）。

```
删除1#风机的塔筒温度点（地址1006）。
```

> 关键点：Agent 经确认按钮执行后——
> ① config.json **双侧成对删除**（接收 addr=1006 与转发 addr=5006 同步消失，不得只删一侧）；
> ② 其余 9 点 shm_id 不变、数据流不中断；被删点的 shm 块被回收（后续新增点可复用该块）；
> ③ 9001 持续监听；对点核验点表中该点消失。

---

## 用例 18：异常——新增点地址与已有点冲突（addr 撞车） ✅

- **测试特性**：新增点的接收地址与已有点占用地址相同（1000 已被「风速」占用）
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成。

```
给1#风机增加一个数据点，地址1000:振动，转发地址5010。
```

> 关键点：Agent 必须拒绝或澄清（可读提示「地址 1000 已被点 风速 占用」），
> **不得静默覆盖已有点的业务字段、不得写入 config、不得重启服务**；
> 用户更换地址后可继续正常流程。新增点**点名撞名**（id 冲突）场景见用例 10——
> 撞名时必须确定性去重（追加序号），仅业务身份（addr 等）一致才视为更新，禁止静默覆盖。

---

## 用例 19：异常——删除不存在的点 ✅

- **测试特性**：删除一个未接入的地址（3000 不在 1000~1009 / 2010 中）；含按点名删除的变体——点名在目标设备上不存在
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成。

```
删除1#风机的地址3000的点。
```

> 关键点：Agent 明确回复「1#风机不存在地址 3000 的点」（宜列出当前点表辅助定位）；
> **不修改配置、不重启服务、不得谎报删除成功**。操作前后 config.json 必须完全一致。

**变体（2026-09-18 补充）：按点名删除不存在的点**

前置状态：用例 1 已完成（1#风机点表 1000~1009：风速、功率、风向、桨叶角度、发电机转速、
齿轮箱油温、塔筒温度、空气温度、空气湿度、大气压强——不含「电网频率」）。

```
删除1#风机的电网频率点
```

> 关键点：与按地址删除等价——Agent 明确回复「1#风机不存在『电网频率』点」（宜列出当前
> 点表辅助定位；若其他设备——如升压站——存在同名点，应提示该点名归属其他设备，
> 不得跨设备误删）；**不修改配置、不重启服务、不得谎报删除成功**；
> 操作前后 config.json 必须完全一致。

---

## 用例 20：异常——追加点缺转发地址（必要项无默认值原则的增量延续） ✅

- **测试特性**：与用例 16 唯一差异是**未提供转发地址**——验证「转发点的 addr 是必要项、
  无默认值、不得自动编造」原则（用例 1 关键点、agent.md 必填项用户提供原则）在增量修改场景的延续
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成。

```
给1#风机增加一个数据点，地址2010:振动。
```

> 关键点：Agent 必须先**询问转发地址**，不得未经用户确认自动编造（含「顺延为 5010」——
> 顺延值也必须作为候选向用户确认后方可采用）；用户补充转发地址后进入用例 16 流程。
> 增量方案在转发地址补齐前不得生成、不得执行。

---

## 用例 21：增加新风机——不同接收端口 + 不同转发端口（正常） ✅

- **测试特性**：以用例 1 状态为前提，新增 2#风机：接收端口 9002（与 9001 不同），
  转发到 II区 9901（与 9900 不同）——验证多实例并存、shm 地址空间隔离与既有链路无损
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成（hnals_wt1 → 9001，转发 → 9900）。

```
再接入2号风机，第三方厂家通过asfp2协议转来2#风机数据，10个点，从1100到1109，分别是1100:风速、1101:功率、1102:风向、1103:桨叶角度、1104:发电机转速、1105:齿轮箱油温、1106:塔筒温度、1107:空气温度、1108:空气湿度、1109:大气压强，使用端口9002。转发到II区服务器127.0.0.1:9901，转发采用asfp2协议，点表6000~6009。
```

> 关键点：① config.json 呈 4 实例并存（server：hnals_wt1+hnals_wt2；client：两个转发实例），
> 新实例 shm_id 自 11 起延续且**与 1#风机区间不相交**、既有点 shm_id 不变；
> ② 9001 与 9002 同时监听，1#风机链路全程不受影响；③ 9900、9901 各自收到对应风机的数据；
> ④ 接收地址（shm addr）跨实例必须全局唯一——用户给出与既有实例重叠的地址段时 Agent 应提示
> （shm 按 addr 索引，重叠即数据互踩）。

---

## 用例 22：增加新风机——不同接收端口 + 相同转发端口（正常） ✅

- **测试特性**：新增 2#风机：接收端口 9002（不同），转发到 II区 **9900（与 1#风机相同的目标端口）**，
  转发点表 6000~6009（与既有 5000~5009 无重叠）——验证同一转发目标端口的多实例并存语义
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成。

```
再接入2号风机，第三方厂家通过asfp2协议转来2#风机数据，10个点，从1100到1109，分别是1100:风速、1101:功率、1102:风向、1103:桨叶角度、1104:发电机转速、1105:齿轮箱油温、1106:塔筒温度、1107:空气温度、1108:空气湿度、1109:大气压强，使用端口9002。转发到II区服务器127.0.0.1:9900（与1#风机相同的目标），转发采用asfp2协议，点表6000~6009。
```

> 关键点：① 同一目标端口允许两个转发实例并存——II区 9900 侧应可见 **2 条独立 TCP 连接**，
> 分别承载两台风机的数据；② **转发点表 key 在同一目标端口上必须全局唯一**——
> 本例 6000~6009 与既有 5000~5009 无交集方可并存；若用户给出的点表与既有转发表重叠
> （如也是 5000~5009），Agent 必须拒绝或提示冲突（II区侧按 key 区分数据，重叠即互踩），
> 不得静默执行；③ 1#风机 → 9900 的既有连接与数据流不受影响。

---

## 用例 23：异常——增加新风机使用相同的接收端口 ✅

- **测试特性**：新增 3#风机但接收端口 **9001（已被 1#风机占用）**，转发端口不同（9901）——
  验证端口冲突的前置检测与可读拒绝
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成（9001 已被 hnals_wt1 监听）。

```
再接入3号风机，第三方厂家通过asfp2协议转来3#风机数据，10个点，从1200到1209，分别是1200:风速、1201:功率、1202:风向、1203:桨叶角度、1204:发电机转速、1205:齿轮箱油温、1206:塔筒温度、1207:空气温度、1208:空气湿度、1209:大气压强，使用端口9001。转发到II区服务器127.0.0.1:9901，转发采用asfp2协议，点表6000~6009。
```

> 关键点：Agent 必须在方案生成前/执行前识别「端口 9001 已被 1#风机 占用」并**可读拒绝或
> 询问改端口**，不得静默生成注定失败（PORT_BIND_FAILED）的方案；config.json 不被破坏，
> hnals_wt1 的 9001 监听与数据流不受影响。兜底语义（若直通执行）：Start 阶段报
> PORT_BIND_FAILED 且全部回滚，不产生半接入状态。

---

## 用例 24：异常——增加新风机使用相同的接收端口与相同的转发端口 ✅

- **测试特性**：新增 4#风机：接收端口 **9001（冲突）**，转发到 **9900（与 1#风机目标端口冲突）**
  且点表 **5000~5009（与 1#风机转发表冲突）**——验证多重冲突的逐项识别
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成。

```
再接入4号风机，第三方厂家通过asfp2协议转来4#风机数据，10个点，从1300到1309，分别是1300:风速、1301:功率、1302:风向、1303:桨叶角度、1304:发电机转速、1305:齿轮箱油温、1306:塔筒温度、1307:空气温度、1308:空气湿度、1309:大气压强，使用端口9001。转发到II区服务器127.0.0.1:9900，转发采用asfp2协议，点表5000~5009。
```

> 关键点：Agent 必须**逐项指出全部冲突**——① 接收端口 9001 已被 1#风机占用；
> ② 转发目标端口 9900 上 1#风机已存在转发实例、且点表 5000~5009 与其转发表重叠
> （同目标端口下转发 key 必须唯一，见用例 22 关键点）；拒绝生成方案/执行，
> config.json 与两台既有风机（1#、及若已按用例 21/22 接入的 2#）的运行状态均不受影响。

---

## 用例 25：删除整台风机（正常） ✅

- **测试特性**：以用例 1 + 用例 21 状态为前提（1#风机 9001→9900、2#风机 9002→9901 均在线），
  整机删除 2#风机——验证双实例成对删除、端口释放、shm 回收与剩余风机无损
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 21 已完成（2#风机在线：接收 9002、转发 → 9901）。

```
删除2号风机。
```

> 关键点：Agent 经确认按钮（摘要须标明将删除的实例与端口）执行后——
> ① config.json **双实例成对删除**：`hnals_wt2` 的接收实例（9002）与转发实例（→9901）
>   同步消失，不得只删一侧（残留空转发实例或只删转发均属缺陷）；
> ② 9002 端口释放（可被后续新风机复用），II区 9901 侧连接断开；
> ③ 2#风机的 shm 块（11~20）被回收，可被后续新增点/新风机复用；
> ④ 1#风机完全无损：9001 持续监听、→9900 转发与数据流不中断、config.json 中其点表
>   与 shm_id 逐字节不变。

---

## 用例 26：删除最后一台风机（边界正常） ✅

- **测试特性**：以仅剩 1#风机 为前提（用例 25 之后），删除唯一的风机——验证删至 0 台的
  合法空态：服务空实例运行、端口全部释放、系统可回到未接入态
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：仅剩 1#风机 在线（9001→9900）。

```
删除1号风机。
```

> 关键点：① 删至 0 台是**合法状态**——config.json 对应 section 为空数组
> （数据路径服务空实例启动为既有语义，c4_fun_00059 TC3），不得报错、不得残留半份配置；
> ② 9001 释放、→9900 连接断开、shm 数据块全部回收；
> ③ Agent 与 Web 正常运行，系统回到「等待首次接入」状态——此后重新执行用例 1 应可完整接入。

---

## 用例 27：异常——删除不存在的风机 ✅

- **测试特性**：删除从未接入的 5 号风机
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 已完成（仅 1#风机 在线）。

```
删除5号风机。
```

> 关键点：Agent 明确回复「5号风机未接入」（宜列出当前已接入风机清单辅助）；
> 不修改配置、不重启任何服务、不得谎报删除成功；操作前后 config.json 完全一致。

---

## 用例 28：异常——批量/模糊删除指令须明确目标与逐台确认 ✅

- **测试特性**：删除指令无明确目标（「把风机都删了」）——破坏性批量操作，验证
  「先列出受影响清单征询明确确认，不得静默全删、也不得模糊不作为」的双向约束
- **修复的版本**：C4H1（2026-09-12 E2E 回归通过：run_cases.py 全量 17/17，含崩溃恢复用例 29）

### 测试输入

前置状态：用例 1 + 用例 21 已完成（1#、2#风机在线）。

```
把风机都删了。
```

> 关键点：① Agent 必须**先列出全部受影响风机**（1#、2# 及各自端口/转发目标）并要求明确确认
> （确认摘要逐台列出，确认按钮出现）——不得跳过确认直接全删，也不得因指令模糊而
> 静默不作为；② 用户确认后执行删除，逐台风机满足用例 25 的成对删除/端口释放/ shm 回收
> 断言；③ 用户取消或未确认时，任何风机均不得被删除。

---

## 协议扩展用户测试用例（modbus / iec104 / influxdb）

> 本节记录 modbus / iec104 / influxdb 三类协议的用户级（会话端到端）测试用例，风格与用例 1~28 一致。
> 用例 29 为 E2E 崩溃恢复用例（记录于 `test/func_case_e2e/README.md`），编号跳过；
> 用例 30~42 已于 2026-09-16~18 实测通过（真实服务接入与异常路径）；用例 43 延后。

**modbus（采集侧：c4_modbus_client 采集 → asfp2 转发）**

## 用例 30：Modbus 采集信息齐全正常接入（变桨控制器 → asfp2 转发） ✅

- **测试特性**：新协议（modbus）采集形态——用户一条文本提供设备 IP、端口、从站号（uid）、10 点
  点表（寄存器地址 + 点名，功能码/数据类型/字节交换以统一说明给出）与转发目标/转发点表；验证
  modbus 必填项（实例 ip/port，每点 addr/uid/fun/type/swap）全部**原样采纳**，方案摘要逐条核对
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
现在需要接入1号风机变桨控制器的数据，厂家在变桨控制器上开放了modbus采集口，设备IP是192.168.110.51，端口502，从站号1。点表10个点：3000:桨叶角度、3002:变桨速度、3004:变桨电机温度、3006:变桨电机电流、3008:后备电源电压、3010:后备电源温度、3012:桨叶1位置、3014:桨叶2位置、3016:桨叶3位置、3018:轮毂温度。除桨叶1/2/3位置是输入寄存器（功能码4）外，其余全是保持寄存器（功能码3），全部32位浮点、字节交换2（swap=2）。我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9900，点表5000~5009。
```

> 关键点：设备 ip、端口、从站号与每点的 addr/uid/fun/type/swap 全部由用户提供（modbus 端口
> **无默认值**——502 系用户显式给出，必须原样采纳）——不得编造、不得改动任何业务字段；
> 方案摘要须逐条列出「寄存器地址 ↔ 点名」映射（3000~3018 偶数地址，FLOAT32 每点占 2 个寄存器）、
> 设备地址（ip:port + 从站号）、各点功能码/类型/字节交换与转发目标/点表，供用户核对；
> 转发协议 asfp2 由用户明确提供并在摘要中列明；config.json **双侧成对**（c4_modbus_client 采集
> 实例 + c4_asfp2_client 转发实例）；接入后对点核验（list_points / 显示卡片）可见 10 个点。

## 用例 31：Modbus 必要项缺失（未给端口与从站号），Agent 询问后接入 ✅

- **测试特性**：与用例 30 同型，但用户**未提供设备端口与从站号（uid）**——两者均为 modbus
  必要项（无默认值）；验证「必填项用户提供、缺失必须询问」原则（用例 1/5 关键点）在新协议的延续
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
现在需要接入1号风机齿轮箱油泵控制器的数据，modbus协议，设备IP是192.168.110.52。点表8个点：4000:油泵运行状态是线圈（功能码1、布尔类型、swap为0），其余7个点4100:油温、4102:油压、4104:油位、4106:泵后压力、4108:电机温度、4110:滤网压差、4112:油流量全是保持寄存器（功能码3）、32位浮点、swap=2。转发到II区127.0.0.1:9901，转发采用asfp2协议，点表5100~5107。
```

Agent 询问缺失信息后，用户补充回答：

```
端口502，从站号都是1。
```

> 关键点：端口与从站号（uid）是 modbus 的必要项（无默认值）——缺失时 Agent 必须先**逐项询问**，
> 禁止按常见端口自动填充、禁止「顺延」推定（即便推定值恰为 502/1 也必须由用户说出）；
> 补齐前不得生成方案、不得执行；补充后原样写入 config.json，其余采纳路径与用例 30 一致
> （含线圈点 fun=1/布尔类型的位采集语义）。

## 用例 32：Modbus 异常——点地址重复 + 功能码非法，可读拒绝且配置不变 ✅

- **测试特性**：点表中寄存器地址 3210 出现两次（不同点名），且点 3212 功能码 5 非法（合法仅
  1/2/3/4）——验证 modbus 点位合法性冲突的**逐项识别**与可读拒绝（用例 18/24 语义延续）
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
现在需要接入1号风机机舱控制柜的数据，modbus协议，设备IP是192.168.110.53，端口502，从站号1。点表6个点：3200:机舱温度、3202:机舱振动、3204:塔基温度、3210:机舱湿度、3210:舱外风向、3212:偏航角度，除3212偏航角度是功能码5外其余都是保持寄存器（功能码3），全部32位浮点、swap=2。转发到II区127.0.0.1:9902，转发采用asfp2协议，点表5200~5205。
```

> 关键点：Agent 必须**逐项指出全部问题并拒绝**——① 寄存器地址 3210 重复（机舱湿度 / 舱外风向）：
> 同一实例内 (uid, fun, addr) 三元组必须唯一；② 点 3212 功能码 5 非法（合法取值 1/2/3/4）。
> 拒绝文案可读（指明点位与非法字段），不得静默修正/去重、不得生成方案、不得写入 config、
> 不得重启服务——操作前后 config.json 必须完全一致；fun=5 非法性若在服务执行层才暴露
> （registry 未枚举合法 fun 值），则须全部回滚（同用例 41 对冲）——重复 addr 部分为
> Agent 层可检；用户更正点表后可继续正常流程。

## 用例 33：Modbus 转发协议缺失——逐侧必供与是非题握手（复用用例 14 语义） ✅

- **测试特性**：接收侧协议（modbus）已声明，转发目标/点表齐全但**未声明转发协议**——转发协议
  与接收协议逐侧独立必供；用户以非模板措辞答复（「就用asfp2吧」）触发问答握手
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
接入1号风机地面环网柜的数据，modbus协议，设备IP是192.168.110.54，端口502，从站号1，点表4个点：3300:环网柜温度、3302:环网柜湿度、3304:电缆头温度、3306:局放值，全是保持寄存器（功能码3）、32位浮点、swap=2。数据要送到II区127.0.0.1:9900，点表5300~5303。
```

Agent 询问转发协议后，用户补充回答：

```
就用asfp2吧。
```

（Agent 以是非题复述候选协议）用户确认：

```
对
```

> 关键点：接收侧协议声明仅对接收侧生效——转发协议缺失时必须询问，禁止沿用接收侧协议（modbus）
> 或按转发目标描述推断；非模板答复不命中便捷通道 → 恰好一轮是非题握手（「转发到II区服务器的
> 协议是 ASFP2 吗？」），用户肯定后放行，接入成功、无死锁（用例 14 场景 A 语义在 modbus 采集
> 链路上的复现）；config.json 双侧成对，转发侧协议为 asfp2。

**iec104（采集侧：c4_iec104_client 采集 → asfp2 转发）**

## 用例 34：IEC104 采集信息齐全正常接入（主变测控装置 → asfp2 转发） ✅

- **测试特性**：用户一条文本提供装置 IP、端口、公共地址与 IOA 点表（遥信/遥测/遥脉混合）及转发
  信息；验证 iec104 必填项（实例 ip/port，每点 addr/IOA）原样采纳；点位**无需预声明数据类型**
  （类型标识由协议帧自带，见 c4_iec104_client.md §2.3/§4.3）
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
现在需要接入1号主变测控装置的数据，装置是IEC104规约，IP是192.168.110.99，端口2404，公共地址1。点表6个点：16385:UAB电压、16386:UBC电压、16387:UAC电压、1:弹簧未储能、2:装置异常、25601:正向有功电度。我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9902，点表5400~5405。
```

> 关键点：ip、端口、IOA 点表为 iec104 必填项，全部**原样采纳**（端口 2404 系用户显式给出，
> 非系统默认填充——2404 默认值已删）；公共地址为用户提供的实例级字段，原样采纳；Agent 不得向
> 用户索要每点数据类型（104 点位仅需 addr:点名，类型由协议帧自带）；方案摘要逐条列出
> 「IOA ↔ 点名」映射（遥信 1/2、遥测 16385~16387、遥脉 25601 混表）与装置地址、转发目标/点表；
> config.json 双侧成对（c4_iec104_client 采集实例 + c4_asfp2_client 转发实例）；从站上送数据后
> 对点核验可见 6 个点。

## 用例 35：IEC104 必要项缺失（未给端口），询问后接入 ✅

- **测试特性**：与用例 34 同型，但用户未提供装置端口——iec104 端口必填（2404 默认值已删），
  验证「必填项用户提供、缺失必须询问」原则在新协议的延续
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
接入2号主变测控装置，IEC104规约，装置IP是192.168.110.199，公共地址1。点表4个点：16385:UAB电压、16386:UBC电压、1:弹簧未储能、25601:正向有功电度。转发到II区127.0.0.1:9903，转发采用asfp2协议，点表5500~5503。
```

Agent 询问缺失信息后，用户补充回答：

```
端口是2404。
```

> 关键点：端口是 iec104 必要项（无默认值）——必须先询问，禁止按协议常见端口自动填充；
> 补充后原样写入 config.json，其余采纳路径与用例 34 一致。

## 用例 36：IEC104 异常——同一装置点表 IOA 重复，可读拒绝且配置不变 ✅

- **测试特性**：点表中 IOA 16385 出现两次（UAB电压 / UAB线电压）——验证「同一实例内 IOA 唯一」
  （c4_iec104_client.md §8 不变式，违反即 INVALID_POINT）的可读拒绝（用例 18/19 语义延续：
  操作前后 config.json 完全一致）
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
现在需要接入1号升压站公用测控装置的数据，IEC104规约，装置IP是192.168.110.101，端口2404，公共地址1。点表4个点：16385:UAB电压、16385:UAB线电压、16386:UBC电压、1:弹簧未储能。转发到II区127.0.0.1:9904，转发采用asfp2协议，点表5600~5603。
```

> 关键点：Agent 可读拒绝（指明 IOA 16385 重复：UAB电压 / UAB线电压；同一实例内信息对象地址
> 必须唯一），不得静默去重/改名、不得生成方案、不得写入 config、不得重启服务——操作前后
> config.json 必须完全一致；用户更正点表后可继续正常流程。

## 用例 37：IEC104 多实例并存——不同装置、完全相同的 IOA 点表（用例 21 变体） ✅

- **测试特性**：以用例 34 + 35 状态为前提（1号、2号主变在线），接入3号主变：不同装置 IP、用户
  指定公共地址 2，**IOA 点表与2号主变完全相同**（同型号装置点表一致是现场常态）——验证 104 点位
  (instance, ioa) 二维映射下「IOA 实例内唯一即可、跨实例同 IOA 合法」
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

前置状态：用例 34、35 已完成（1号主变 → 9902，2号主变 → 9903）。若用例 36 场景已执行且用户更正点表完成接入，环境中
另有升压站实例（→9904，5600~5603）；本用例 3号主变 转发目标 9904 与其 key 不重叠
（5700~5703），并存合法（同用例 22 语义）。

```
再接入3号主变测控装置，IEC104规约，装置IP是192.168.110.102，端口2404，公共地址2。点表与2号主变完全一样：16385:UAB电压、16386:UBC电压、1:弹簧未储能、25601:正向有功电度。转发到II区127.0.0.1:9904，转发采用asfp2协议，点表5700~5703。
```

> 关键点：① 同一 IOA 段跨实例不构成冲突——104 点位按 (instance, ioa) 二维映射
> （c4_iec104_client.md §5.1），IOA 只需实例内唯一，各实例 shm_id 独立分配、区间不相交；
> ② config.json 呈多实例并存（三台主变采集 + 各自转发），既有 1号/2号主变的链路、点表与
> shm_id 完全不变；③ 转发目标 9904 与既有 9902/9903 互不干扰，各自收到对应装置的数据
> （含用例 36 更正接入后的升压站在 9904 上的并存，若在）。

**influxdb（写入/转发侧：c4_asfp2_server 接收 → c4_influxdb_client 写入 InfluxDB）**

## 用例 38：InfluxDB 入库信息齐全正常接入（asfp2 接收 + 写入 InfluxDB） ✅

- **测试特性**：数据落地形态（入库而非外转）——用户一条文本提供接收侧（asfp2、端口 9001、
  10 点点表）与写入侧全部必要项（url/token/org/bucket）及逐点映射（measurement 统一、field 与
  点名对应、type 统一 float）；「写入InfluxDB」命中写入侧协议声明的便捷通道（用例 14 模板句语义）
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
现在需要接入1号风机的数据并直接入库。第三方厂家通过asfp2协议给我们转来1#风机数据，10个点，从1000到1009，分别是1000:风速、1001:功率、1002:风向、1003:桨叶角度、1004:发电机转速、1005:齿轮箱油温、1006:塔筒温度、1007:空气温度、1008:空气湿度、1009:大气压强，使用端口9001。数据写入我们的InfluxDB时序库：写入地址http://172.16.109.12:8086，token是hnals-influx-2026，org是activesys，bucket是hnals。10个点全部写进wind_turbine这个measurement，字段名跟点名对应（windspeed、power、wind_dir、pitch_angle、gen_speed、gearbox_oil_temp、tower_temp、air_temp、humidity、pressure），类型统一float。
```

> 关键点：① config.json **双侧成对**：c4_asfp2_server 接收实例（9001、10 点 addr:点名 原样采纳）
> + c4_influxdb_client 入库实例——**不得生成 asfp2 转发实例**（用户声明的是入库不是外转）；
> ② 写入侧 url/token/org/bucket 四项必要项全部原样采纳（空字符串视为缺失，本例全部由用户提供）；
> ③ 入库点位 key 逐点对应采集点（{采集实例id}.{点id}），measurement=wind_turbine、field 与
> type=float 原样采纳；④ 数据面验证：asfp2 侧送数 → shm 更新 → InfluxDB 按
> measurement=wind_turbine 查到 10 个字段，数值与毫秒时间戳和 shm 对点核验一致；
> ⑤ 写入侧协议声明完成（便捷通道命中或至多一轮是非题）——输入句「数据写入我们的InfluxDB
> 时序库」按「写入X」等宽义匹配命中便捷通道；若实现为紧邻匹配则可能发生一轮是非题确认，
> 不作为缺陷（用例 14 便捷通道语义，不留「无确认轮」绝对断言）。

## 用例 39：InfluxDB 必要项缺失（未给 bucket），询问后接入 ✅

- **测试特性**：与用例 38 同型，但未提供 bucket——url/token/org/bucket 均为连接必要项
  （空字符串视为缺失，无默认值）；验证缺失必询问原则在写入侧的延续
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

```
现在需要接入1号风机的数据并直接入库。第三方厂家通过asfp2协议给我们转来1#风机数据，10个点，从1000到1009，分别是1000:风速、1001:功率、1002:风向、1003:桨叶角度、1004:发电机转速、1005:齿轮箱油温、1006:塔筒温度、1007:空气温度、1008:空气湿度、1009:大气压强，使用端口9001。数据写入InfluxDB：写入地址http://172.16.109.12:8086，token是hnals-influx-2026，org是activesys。10个点全部写进wind_turbine，字段名跟点名对应，类型统一float。
```

Agent 询问缺失信息后，用户补充回答：

```
bucket是hnals。
```

> 关键点：bucket（连同 url/token/org）是 InfluxDB 连接必要项——缺失时必须先询问，禁止生成方案、
> 禁止以任何默认值填充；补充后原样写入 config.json，其余采纳与验证路径同用例 38。

## 用例 40：InfluxDB 写入目标不可达——接入成功、后台重试、恢复后续写 ✅

- **测试特性**：与用例 38 信息完全一致，但测试注入 InfluxDB 服务不可达（172.16.109.12:8086
  当机/网络隔离）——验证「start 成功返回、连接/写入失败属数据面运行时事件、不作为接入错误」的
  T0 语义（与 modbus/iec104「连接失败不导致 start 报错」同源），及恢复后的续写行为
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

前置状态：InfluxDB 服务（172.16.109.12:8086）被人为停止或网络不可达；环境须**回滚至用例 38
之前（或全新环境）**，再重放与用例 38 完全相同的输入（略）——否则接收端口 9001 被用例 38
实例占用，start 将报 PORT_BIND_FAILED，关键点 ① 不成立。

> 关键点：① 接入流程正常完成——c4_influxdb_client 的 start 返回 success，网络错误**不作为工具
> 错误**、不回滚接入、不删除配置；Agent 不得谎报「数据已入库」——接入结果只描述配置层面
> （实例、点位、端口），数据写入属 MCP 数据面运行时事件，agent 不感知、不说明（既不得谎报
> 已入库，也无需说明写入暂败或重试）；② 不可达期间按指数退避重试（≤ retries 次），重试耗尽丢弃该批次并继续（管道不阻塞，
> 接收侧 9001 与 shm 数据链路不受影响）；③ InfluxDB 恢复后**新数据正常续写**、查询可见；
> 重试耗尽期间被丢弃的批次**不回溯补写**（best-effort 语义：last_seen 已推进）；
> ④ 除新增实例外，config.json 其余部分与既有服务状态不变。

## 用例 41：InfluxDB 入库点位重复引用同一采集点，可读拒绝且配置不变 ✅

- **测试特性**：以用例 38 状态为前提，用户要求把同一采集点（风速）在同一入库实例内再映射一份到
  另一个 measurement——同一实例内两个点位引用同一采集点 key 即 shm_id 重复
  （c4_influxdb_client.md §2.3「同一实例内 shm_id 不得重复」，start 校验 INVALID_POINT），
  验证可读拒绝与配置不变
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

前置状态：用例 38 已完成（1#风机 9001 在线、hnals 入库实例运行中）。

```
给1号风机的入库再加一条：风速除了wind_turbine，也同步写一份到wind_anomaly这个measurement，字段也叫windspeed，类型float。
```

> 关键点：Agent 可读拒绝——同一入库实例内风速点已被引用，再次映射将造成同实例 shm_id 重复
> （INVALID_POINT：数据点配置无效）；不得静默追加、不得写入 config、不得重启服务，操作前后
> config.json 必须完全一致（若在执行层拦截则须全部回滚、不留半接入状态）；确需双 measurement
> 落地时可作为**独立入库实例**另行接入（多路下游共用同一采集点 key 是设计支持的形态，
> 不在本用例验证范围）。

## 用例 42：Modbus 采集设备不可达——start 成功、Agent 不谎报、恢复后数据出现 ✅

- **测试特性**：全新环境接入变桨控制器（modbus），设备 ip 指向一个未上电地址——验证「连接失败
  不导致 start 报错、接入流程正常完成（start 工具成功返回）」的统一语义
  （与用例 40 InfluxDB 写入侧「目标不可达」同源，采集侧协议差异不作断言锚点）
- **修复的版本**：C4H1（2026-09-16~18 实测通过）

### 测试输入

前置状态：全新环境（无任何已接入实例）。变桨控制器未上电，接入输入中的设备 ip 即指向该
未上电地址。

```
现在需要接入1号风机变桨控制器的数据，厂家在变桨控制器上开放了modbus采集口，设备IP是192.168.110.51，端口502，从站号1。点表10个点：3000:桨叶角度、3002:变桨速度、3004:变桨电机温度、3006:变桨电机电流、3008:后备电源电压、3010:后备电源温度、3012:桨叶1位置、3014:桨叶2位置、3016:桨叶3位置、3018:轮毂温度。除桨叶1/2/3位置是输入寄存器（功能码4）外，其余全是保持寄存器（功能码3），全部32位浮点、字节交换2（swap=2）。我们需要将这些数据转发到II区服务器上，转发采用asfp2协议，目标地址是127.0.0.1:9900，点表5000~5009。
```

> 关键点：① c4_modbus_client 的 start 返回 success（isError=false）——设备不可达属数据面
> 运行时事件，不得作为连接类 tool 错误上报、不得回滚接入（c4_modbus_client.md §3.2，
> c4_architecture.md §3.3.1 生命周期工具返回时机语义）；② Agent 告知用户「接入完成、设备
> 暂不可达、将自动重连」等通信层断言——agent 只基于 read_points 工具结果汇报：**不得谎报
> 数据已采集**，无数据期间点位显示「暂无数据」而非零值，无需说明设备可达性或重连行为；
> ③ 设备上电后服务自动重连（c4_modbus_client.md §4.4 实现级重连参数——Modbus 规范未定义
> 重连参数，实现缺省、非必填、永不询问），重连成功恢复轮询后，对点核验（list_points /
> 显示卡片）可见 10 个点的真实值；④ 断言不锚定具体定时器参数值（重连周期属协议差异，
> 锚定 c4_architecture.md §3.3.1 统一语义：start 成功 + 运行期自动恢复）。
> 设计参考：c4_modbus_client.md §3.2/§4.4/§6.1 + c4_architecture.md §3.3.1。

## 用例 43：InfluxDB 合法多下游入库——两个 influxdb 实例引用相同采集 key ⏸ 延后（本轮不支持）

> **2026-09-16 裁定：本轮不支持多转发/多下游用例，本用例延后。**
> 原因：多实例读侧共享 shm_id 的语义在架构设计文档与 MCP 工具契约（start 的 instance_id
> 校验模式与 config 内实例 id 命名不一致）中存在模糊地带，需先在设计层对齐后再启用本用例。
> 保留用例文本作为将来支持时的验收依据。

- **测试特性**：以用例 38 状态为前提，用户要求把同一批采集数据（这10个点）再写一份到
  另一个 bucket（url 同、bucket 换成 wind_history）——经**第二个 c4_influxdb_client 实例**
  落地，两实例点位携带与 writer 相同的 shm_id（读侧共享合法，c4_shm_manager.md 按 key 匹配）；
  与用例 41 的非法变体（同一实例内重复引用）相对照
- **修复的版本**：（延后，待多下游设计对齐后回填）

### 测试输入

前置状态：用例 38 已完成（1#风机 9001 在线、hnals 入库实例运行中）。

```
这10个点都写一份到另一个bucket：url同，还是http://172.16.109.12:8086，token是hnals-influx-2026，org是activesys，bucket换成wind_history，measurement和字段名跟wind_turbine那边一样，类型统一float。
```

> 关键点：① Agent 创建**第二个 c4_influxdb_client 实例**（独立实例、bucket=wind_history），
> 而非在用例 38 实例内追加点位——同实例内重复引用同一采集 key 非法，该非法变体已由用例 41
> 覆盖；② 新实例全部点位与用例 38 实例引用相同的采集 key、携带与 writer 相同的 shm_id
> （多路下游读侧共享合法，c4_shm_manager.md key 匹配语义）；③ 数据面验证：asfp2 侧送数后，
> hnals 与 wind_history 两个 bucket 均可查询到数据（measurement=wind_turbine，字段与数值
> 一致）；④ 用例 38 实例不受影响——既有 hnals 入库实例的点位、shm_id 与运行状态完全不变。
