{
  "service_type": "c4_influxdb_client",
  "display_name": "InfluxDB 数据存储",
  "role": "reader",
  "protocols": [
    {
      "protocol": "influxdb",
      "description": "InfluxDB 数据入库客户端，从共享内存读取数据后写入 InfluxDB 时序数据库",
      "selection_rules": [
        {
          "condition": "target.role == 'store'",
          "description": "数据入库场景（写入 InfluxDB 时序数据库）"
        }
      ]
    }
  ],
  "point_schema": {
    "fields": [
      {
        "name": "measurement",
        "type": "string",
        "description": "measurement 名（表名）"
      },
      {
        "name": "field",
        "type": "string",
        "description": "field 名（字段名）"
      },
      {
        "name": "type",
        "type": "string",
        "description": "数据类型（枚举见 point_field_hints.type）"
      }
    ],
    "identity_fields": [
      "measurement",
      "field"
    ]
  },
  "config_schema": {
    "fields": {
      "url": {
        "type": "string",
        "description": "InfluxDB 写入端点 URL（如 <用户提供的写入端点 URL>）"
      },
      "token": {
        "type": "string",
        "description": "认证 token（必填，空字符串视为缺失；1.x 未启用认证时填任意非空值）"
      },
      "org": {
        "type": "string",
        "description": "组织名（必填，空字符串视为缺失；1.x 时填任意非空值，被忽略）"
      },
      "bucket": {
        "type": "string",
        "description": "bucket 名（必填，空字符串视为缺失；1.x 时填数据库名）"
      },
      "precision": {
        "type": "string",
        "default": "ms",
        "description": "时间戳精度：ns/us/ms/s（默认 ms）"
      },
      "batch_size": {
        "type": "integer",
        "default": 5000,
        "description": "单次 flush 行数上限（0 或负数视为非法）"
      },
      "flush_interval": {
        "type": "integer",
        "default": 1000,
        "description": "flush 间隔（毫秒，0 禁用时间触发）"
      },
      "timer": {
        "type": "integer",
        "default": 100,
        "description": "共享内存轮询周期（毫秒）"
      },
      "gzip": {
        "type": "integer",
        "default": 1,
        "description": "是否启用 gzip 压缩请求体（1=启用，0=关闭）"
      },
      "t0": {
        "type": "integer",
        "default": 30,
        "description": "HTTP 请求超时（秒）"
      },
      "retries": {
        "type": "integer",
        "default": 3,
        "description": "flush 失败最大重试次数（0 不重试）"
      }
    }
  },
  "binary_path": "/usr/local/bin/c4_influxdb_client",
  "error_mappings": {
    "ALREADY_RUNNING": "服务已在运行中，无需重复启动",
    "CONFIG_PATH_MISSING": "配置文件路径无效，请检查 Agent 部署是否正确",
    "CONFIG_PARSE_ERROR": "配置文件格式错误，请检查配置内容",
    "INVALID_CONFIG": "实例配置无效，请检查数据库地址或认证信息",
    "INVALID_POINT": "数据点配置无效，请检查数据点类型或名称",
    "SHM_CORRUPTED": "数据存储异常，请联系管理员检查共享内存状态",
    "SHM_OPEN_FAILED": "无法访问数据存储，请检查共享内存配置",
    "SHM_ID_NOT_ASSIGNED": "数据点未分配通道，请重新生成接入方案"
  },
  "prompt_hints": {
    "protocol_match": {
      "aliases": [
        "InfluxDB",
        "influxdb",
        "influx 时序数据库"
      ],
      "rejected_variants": []
    },
    "point_field_hints": {
      "measurement": {
        "description": "measurement 名（表名），相当于关系数据库的表名，通常按设备或数据类别命名",
        "extraction": "可由场站缩写确定性推导（阶段 1 已确认数据）；用户明确给出时以用户为准；推导值在方案展示中标注供确认"
      },
      "field": {
        "description": "field 名（字段名）",
        "extraction": "常见表述：按点名作为字段名"
      },
      "type": {
        "description": "数据类型",
        "enum": {
          "float": "浮点数",
          "int": "有符号整数",
          "uint": "无符号整数",
          "bool": "布尔量"
        },
        "extraction": "可由源点数据类型确定性推导（类型映射：modbus 浮点→float、整数→int/uint、位→bool）；用户明确给出时以用户为准；推导值在方案展示中标注供确认"
      }
    },
    "connection_hints": [
      "url/token/org/bucket 为连接必要项，接入前须全部确认",
      "1.x 版本未启用认证时：token/org 填任意非空值，bucket 填数据库名",
      "precision/batch_size/flush_interval/gzip/t0/retries/timer 等字段有默认值，无需向用户询问"
    ],
    "display": "InfluxDB 数据入库客户端：从共享内存读取数据写入 InfluxDB 时序数据库"
  }
}